// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Assuming you're using AGP 8.0 or later and Kotlin 1.8.0 or later
    // If you use version catalogs (libs.versions.toml), this would look different
    id("com.android.application") version "8.12.0" apply false // Use your actual AGP version
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false // Use your actual Kotlin version
}